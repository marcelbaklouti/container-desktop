import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Containers")
            .font(.largeTitle)
            .foregroundStyle(.secondary)
            .frame(minWidth: 800, minHeight: 500)
    }
}

#Preview {
    ContentView()
}
